<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.jdbi.generator</groupId>
  <artifactId>jdbi-generator</artifactId>
  <name>JDBI Generator</name>
  <version>1.0.0</version>
  <description>JDBI Generator</description>
  <organization>
    <name>tonibauti</name>
    <url>https://github.com/ableon</url>
  </organization>
  <build>
    <resources>
      <resource>
        <directory>${project.basedir}/src/main/resources</directory>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
        <configuration>
          <source>${maven.compiler.source}</source>
          <target>${maven.compiler.target}</target>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.2.4</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <finalName>${project.artifactId}-${project.version}</finalName>
              <shadedArtifactAttached>false</shadedArtifactAttached>
              <transformers>
                <transformer>
                  <manifestEntries>
                    <Main-Class>${main.class}</Main-Class>
                    <Build-Number>${project.version}</Build-Number>
                    <Created-By>${project.organization.name}</Created-By>
                    <Built-By>${project.organization.name}</Built-By>
                    <Add-Opens>java.base/jdk.internal.loader</Add-Opens>
                  </manifestEntries>
                </transformer>
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.12</version>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <artifactId>hamcrest-core</artifactId>
          <groupId>org.hamcrest</groupId>
        </exclusion>
      </exclusions>
    </dependency>
  </dependencies>
  <properties>
    <banner.version>0.0.8</banner.version>
    <jakarta-validator.version>4.0.1</jakarta-validator.version>
    <commons-cli.version>1.5.0</commons-cli.version>
    <maven.compiler.target>1.8</maven.compiler.target>
    <freemarker.version>2.3.31</freemarker.version>
    <jackson.version>2.13.0</jackson.version>
    <main.class>org.jdbi.generator.main.Main</main.class>
    <hibernate-validator.version>7.0.1.Final</hibernate-validator.version>
    <maven.compiler.source>1.8</maven.compiler.source>
  </properties>
</project>
